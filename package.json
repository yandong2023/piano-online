{
  "name": "piano_online",
  "version": "1.0.0",
  "description": "Interactive bilingual virtual piano and song-learning website.",
  "main": "piano.js",
  "scripts": {
    "start": "http-server -p 8000 -c-1",
    "generate:songs": "node scripts/generate-song-pages.mjs",
    "test": "npm run generate:songs && node --check js/main.js && node --check js/song-discovery.js && node --check js/song-page.js"
  },
  "keywords": ["virtual piano", "online piano", "piano songs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
