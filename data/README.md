# Song data

`song-catalog.mjs` is the canonical runtime source for song titles, slugs, difficulty, tempo, key signature, descriptions, and note sequences.

`seoEnabled` controls whether a song is allowed to have a public indexed song page. Keep modern copyrighted songs disabled until publishing rights are reviewed.
